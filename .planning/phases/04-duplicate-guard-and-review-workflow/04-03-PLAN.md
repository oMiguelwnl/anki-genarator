---
phase: 04-duplicate-guard-and-review-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
  - ankideck_generator/tests/test_deck_export.py
autonomous: true
requirements:
  - COMP-03
must_haves:
  truths:
    - `quality_report.json` highlights accepted-card rate, duplicate reject rate, and accepted-with-corrections counts at the top level.
    - Duplicate diagnostics split exact versus near duplicates overall and by level.
    - Existing `.apkg`, metadata, quality report, and review queue outputs still exist after the richer report changes.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: acceptance-quality and duplicate-diagnostics report sections
      contains: _write_quality_outputs
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: quality report section regressions
      contains: test_write_quality_outputs_reports_duplicate_diagnostics_sections
    - path: ankideck_generator/tests/test_deck_export.py
      provides: output compatibility regression
      contains: test_deck_export
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: output/quality_report.json
      via: _write_quality_outputs
      pattern: duplicate_diagnostics
    - from: ankideck_generator/core/deck_builder.py
      to: output/metadata.json
      via: existing export path remains unchanged while report schema grows
      pattern: metadata.json
---

<objective>
Publish accepted-card and duplicate-quality diagnostics in `quality_report.json` without breaking the existing export/output contract.

Purpose: Implement D-13 through D-16 and COMP-03 so quality gains become visible in structured outputs while `.apkg`, metadata, review queue, and quality report generation remain intact.
Output: richer report sections for acceptance quality, duplicate diagnostics, and review diagnostics plus regression coverage that protects export compatibility.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-duplicate-guard-and-review-workflow/04-CONTEXT.md
@.planning/phases/04-duplicate-guard-and-review-workflow/04-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/tests/test_deck_builder.py
@ankideck_generator/tests/test_deck_export.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
class BuildStats:
    cards: list[CardData] = field(default_factory=list)
    accepted_by_level: Counter[int] = field(default_factory=Counter)
    attempted_by_level: Counter[int] = field(default_factory=Counter)
    validation_counter: Counter[str] = field(default_factory=Counter)
    discard_counter: Counter[str] = field(default_factory=Counter)
    corrected_accepted_count: int = 0
    needs_review_items: list[dict[str, object]] = field(default_factory=list)

def _write_quality_outputs(self, run: RunConfig, stats: BuildStats) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish dedicated acceptance, duplicate, and review report sections</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/test_deck_builder.py
    - .planning/phases/04-duplicate-guard-and-review-workflow/04-CONTEXT.md
    - .planning/PROJECT.md
  </read_first>
  <behavior>
    - Test 1: `quality_report.json` exposes top-line `accepted_card_rate`, `duplicate_reject_rate`, and `accepted_with_corrections` per D-13.
    - Test 2: `duplicate_diagnostics` splits `exact` versus `near` counts overall and by level per D-14.
    - Test 3: `acceptance_quality` splits `clean_accepts` versus `corrected_accepts`, while `review_diagnostics` remains separate from duplicate counters per D-15/D-16.
  </behavior>
  <action>Refactor `ankideck_generator/core/deck_builder.py::_write_quality_outputs()` to keep existing top-level compatibility fields but add three dedicated sections: `acceptance_quality`, `duplicate_diagnostics`, and `review_diagnostics`. `acceptance_quality` must include exact keys `accepted_cards`, `accepted_card_rate`, `clean_accepts`, and `corrected_accepts`. `duplicate_diagnostics` must include exact keys `overall`, `by_level`, `exact_rejects`, `near_rejects`, and `duplicate_reject_rate`, using the new exact/near reason codes from Plan 04-01. `review_diagnostics` must summarize rejected queue volume, top reason codes, and separated `hard_validation_errors`/`review_flags` counts from Plan 04-02. Preserve the current flat counters (`cards`, `attempted_by_level`, `accepted_by_level`, `needs_review`, `accepted_with_corrections`) so downstream compatibility is maintained while the richer sections become the new primary contract.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains `acceptance_quality`
    - `ankideck_generator/core/deck_builder.py` contains `duplicate_diagnostics`
    - `ankideck_generator/core/deck_builder.py` contains `review_diagnostics`
    - `ankideck_generator/core/deck_builder.py` contains `accepted_card_rate`
    - `ankideck_generator/core/deck_builder.py` contains `duplicate_reject_rate`
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_write_quality_outputs_reports_duplicate_diagnostics_sections`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs and duplicate_diagnostics_sections"</automated>
  </verify>
  <done>`quality_report.json` highlights acceptance quality and duplicate diagnostics in dedicated sections without hiding the existing counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove output compatibility for deck export and structured reports</name>
  <files>ankideck_generator/tests/test_deck_builder.py, ankideck_generator/tests/test_deck_export.py</files>
  <read_first>
    - ankideck_generator/tests/test_deck_builder.py
    - ankideck_generator/tests/test_deck_export.py
    - AGENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Test 1: the report writer still creates both `quality_report.json` and `review_queue.json` under the configured output paths.
    - Test 2: the existing `.apkg` export regression still passes, proving this phase did not break deck packaging or metadata generation per COMP-03.
  </behavior>
  <action>Add or update focused regressions so `ankideck_generator/tests/test_deck_builder.py` asserts the richer report still writes both configured JSON outputs, and keep `ankideck_generator/tests/test_deck_export.py::test_deck_export` passing unchanged as the compatibility sentinel for `.apkg` plus metadata generation. Do not rewrite export behavior in this phase; this task exists to lock the existing output contract while the reporting schema grows.</action>
  <acceptance_criteria>
    - `ankideck_generator/tests/test_deck_builder.py` contains `quality_report.json`
    - `ankideck_generator/tests/test_deck_builder.py` contains `review_queue.json`
    - `ankideck_generator/tests/test_deck_export.py` contains `test_deck_export`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs" && python -m pytest ankideck_generator/tests/test_deck_export.py::test_deck_export -q</automated>
  </verify>
  <done>The richer quality report coexists with the existing JSON outputs and does not break deck export compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BuildStats counters → `quality_report.json` | Internal counters become user-facing quality metrics |
| richer reporting schema → existing export/output consumers | New sections must not break legacy consumers expecting existing files/counters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | R | quality report metrics | mitigate | Derive acceptance/duplicate metrics directly from BuildStats counters and rejected-item reason codes so reported rates are reproducible. |
| T-04-08 | D | downstream output consumers | mitigate | Preserve existing top-level counters and keep `test_deck_export` as a compatibility sentinel while adding richer sections. |
| T-04-09 | I | review diagnostics | mitigate | Aggregate review flags separately from hard validation errors so report readers do not overexpose or misclassify diagnostic detail. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs and duplicate_diagnostics_sections"`
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs" && python -m pytest ankideck_generator/tests/test_deck_export.py::test_deck_export -q`
</verification>

<success_criteria>
- `quality_report.json` visibly separates acceptance quality, duplicate diagnostics, and review diagnostics.
- Exact versus near duplicate rejects are counted overall and by level.
- `.apkg`, metadata, review queue, and quality report outputs still exist after the report changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-duplicate-guard-and-review-workflow/04-03-SUMMARY.md`
</output>
