---
phase: 04-duplicate-guard-and-review-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - ankideck_generator/core/models.py
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - EVAL-03
must_haves:
  truths:
    - Rejected duplicate and review items in `output/review_queue.json` include enough context to explain the decision without replaying logs.
    - Queue artifacts separate hard validation failures from softer review flags while preserving specific reason codes.
    - Queue provenance preserves both field-level providers and the decisive provider/model or duplicate-guard source for the final rejection.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: structured rejected-item payload assembly
      contains: _record_log
    - path: ankideck_generator/core/models.py
      provides: log/evidence fields needed for queue payloads
      contains: LogRecord
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: review queue artifact regressions
      contains: test_write_quality_outputs_duplicate_review_queue_artifact_shape
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: output/review_queue.json
      via: stats.needs_review_items
      pattern: needs_review_items
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/models.py
      via: LogRecord evidence/provenance fields
      pattern: LogRecord
---

<objective>
Reshape rejected-item artifacts so duplicate and review failures are audit-ready, provenance-rich, and explicit about hard-vs-soft diagnostics.

Purpose: Implement D-08 through D-12 plus QUAL-02/QUAL-03/EVAL-03 while preserving the queue-first autonomous flow from earlier phases.
Output: structured review-queue payloads with card snapshots, changed-fields evidence, duplicate evidence, separated validation/review diagnostics, and decisive-source provenance.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-duplicate-guard-and-review-workflow/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-contextual-lexical-review/03-CONTEXT.md
@.planning/phases/04-duplicate-guard-and-review-workflow/04-01-PLAN.md
@ankideck_generator/core/models.py
@ankideck_generator/core/deck_builder.py
@ankideck_generator/tests/test_deck_builder.py

<interfaces>
From `ankideck_generator/core/models.py`:

```python
class LogRecord(BaseModel):
    validations: list[str] = Field(default_factory=list)
    review_notes: list[str] = Field(default_factory=list)
    reason_codes: list[str] = Field(default_factory=list)
    before: dict[str, Any] = Field(default_factory=dict)
    after: dict[str, Any] = Field(default_factory=dict)
    providers: dict[str, str] = Field(default_factory=dict)
    provider: str | None = None
    model: str | None = None
```

From `ankideck_generator/core/deck_builder.py`:

```python
class BuildStats:
    needs_review_items: list[dict[str, object]] = field(default_factory=list)

def _record_log(self, logger: JsonLogger, stats: BuildStats, log_record: LogRecord) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit audit-ready review queue payloads with duplicate evidence</name>
  <files>ankideck_generator/core/models.py, ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/models.py
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/test_deck_builder.py
    - .planning/phases/04-duplicate-guard-and-review-workflow/04-CONTEXT.md
    - .planning/phases/03-contextual-lexical-review/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: duplicate-queued items preserve `focus`, `sentence`, `definition`, `translation`, and `changed_fields` per D-09/D-10.
    - Test 2: duplicate-queued items surface structured duplicate evidence with `kind`, `matched_sentence`, and `similarity` per D-06/D-12.
    - Test 3: rejected lexical-review items continue using the same queue channel instead of creating a blocking runtime review state, preserving the prior phase posture.
  </behavior>
  <action>Add explicit queue-artifact assembly in `ankideck_generator/core/deck_builder.py::_record_log()` that writes one rejected-item payload shape for all rejected outcomes. The payload must include `card_snapshot` with `focus`, `sentence`, `definition`, `translation`, `source_definition`, and `ipa`; `changed_fields` as a sorted list of keys present in `after`; `hard_validation_errors` copied from `validations`; `review_flags` copied from `review_notes`; `reason_codes`; `duplicate_evidence` from the Plan 04-01 contract when present; and `field_providers` copied from `providers`. Preserve the existing queue-only-for-rejected behavior from Phase 01/03; do not add accepted cards or a runtime human-review lane. If needed, extend `LogRecord` with one explicit evidence/provenance field rather than overloading unrelated keys.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains `card_snapshot`
    - `ankideck_generator/core/deck_builder.py` contains `changed_fields`
    - `ankideck_generator/core/deck_builder.py` contains `hard_validation_errors`
    - `ankideck_generator/core/deck_builder.py` contains `review_flags`
    - `ankideck_generator/core/deck_builder.py` contains `duplicate_evidence`
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_write_quality_outputs_duplicate_review_queue_artifact_shape`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "review_queue and duplicate_review_queue_artifact_shape"</automated>
  </verify>
  <done>`review_queue.json` items are self-explanatory, duplicate-aware, and preserve the textual evidence needed for audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve decisive-source and field-level provenance in rejected artifacts</name>
  <files>ankideck_generator/core/models.py, ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/models.py
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/test_deck_builder.py
    - AGENTS.md
  </read_first>
  <behavior>
    - Test 1: lexical-review rejections retain field-level provider provenance plus the decisive reviewer provider/model per D-11.
    - Test 2: duplicate rejections emit a decisive source object with `stage="duplicate_guard"`, `provider="duplicate_guard"`, and the configured similarity threshold/model label even when earlier lexical fields came from other providers.
    - Test 3: hard validation failures and soft review flags remain separate in the queue payload, satisfying QUAL-02 and EVAL-03.
  </behavior>
  <action>Extend rejected-item payloads with a `decision_source` object containing exact keys `stage`, `provider`, and `model`. For duplicate decisions, set `stage` to `duplicate_guard`, `provider` to `duplicate_guard`, and `model` to a deterministic label that includes the near-duplicate threshold (`sequence_matcher@0.90`) per D-11. For review-driven rejections, derive `decision_source` from `log_record.provider` and `log_record.model`, while keeping `field_providers` untouched so field-level provenance survives. Keep `hard_validation_errors` and `review_flags` as distinct arrays in the serialized payload; do not collapse them back into `reason_codes` or one mixed list.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains `decision_source`
    - `ankideck_generator/core/deck_builder.py` contains `duplicate_guard`
    - `ankideck_generator/core/deck_builder.py` contains `sequence_matcher@0.90`
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_review_queue_preserves_decision_source_and_field_providers`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "decision_source and field_providers"</automated>
  </verify>
  <done>Rejected artifacts preserve both field-level provenance and the decisive source of the final duplicate/review rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LogRecord evidence → `review_queue.json` | Internal evidence is serialized into user-facing audit artifacts |
| provider provenance → final rejection source | Multi-provider field history is condensed into one decisive-source object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | R | rejected artifact payloads | mitigate | Serialize `card_snapshot`, `changed_fields`, `reason_codes`, and `duplicate_evidence` so rejections remain auditable without replaying logs. |
| T-04-05 | I | provenance fields | mitigate | Preserve `field_providers` separately from `decision_source` so decisive-source compaction does not erase field-level provider history. |
| T-04-06 | T | diagnostics arrays | mitigate | Keep `hard_validation_errors` and `review_flags` in separate arrays so downstream tooling cannot confuse hard rejects with softer review signals. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "review_queue and duplicate_review_queue_artifact_shape"`
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "decision_source and field_providers"`
</verification>

<success_criteria>
- `review_queue.json` preserves textual state, changed fields, duplicate evidence, and provenance for every rejected item.
- Hard validation errors and review flags are serialized as distinct concepts.
- Duplicate decisions identify `duplicate_guard` as the decisive source while retaining per-field provider history.
</success_criteria>

<output>
After completion, create `.planning/phases/04-duplicate-guard-and-review-workflow/04-02-SUMMARY.md`
</output>
