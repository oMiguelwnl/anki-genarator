---
phase: 02-ai-sentence-generation
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json
  - ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json
  - ankideck_generator/tests/test_providers.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - AIG-01
  - AIG-02
  - AIG-03
  - AIG-04
must_haves:
  truths:
    - Structured AI sentence behavior is reproducible from fixture payloads, not only inline mocks.
    - Malformed, focus-missing, and low-yield cases stay covered after prompt/schema revisions.
    - AI-first routing and salvage-only fallback are protected by regression tests.
  artifacts:
    - path: ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json
      provides: valid structured AI batch fixture
      contains: candidates
    - path: ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json
      provides: malformed structured AI batch fixture
      contains: candidates
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: fixture-driven routing regressions
      contains: test_process_word_uses_web_fallback_only_after_malformed_ai_batch
  key_links:
    - from: ankideck_generator/tests/test_providers.py
      to: ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json
      via: fixture-backed payload parsing
      pattern: ai_sentence_candidates
    - from: ankideck_generator/tests/test_deck_builder.py
      to: ankideck_generator/core/deck_builder.py
      via: AI-first and fallback orchestration assertions
      pattern: build_sentence
---

<objective>
Lock Phase 2 behavior with fixture-based regressions for structured AI batches and fallback outcomes.

Purpose: Make malformed-output and bounded-fallback behavior durable across future prompt/schema changes.
Output: JSON fixtures plus provider/deck-builder regressions that exercise valid and invalid AI batches.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ai-sentence-generation/02-CONTEXT.md
@.planning/phases/02-ai-sentence-generation/02-RESEARCH.md
@ankideck_generator/tests/test_providers.py
@ankideck_generator/tests/test_deck_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical structured sentence fixtures</name>
  <files>ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json, ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json, ankideck_generator/tests/test_providers.py</files>
  <read_first>
    - ankideck_generator/tests/test_providers.py
    - ankideck_generator/core/models.py
    - .planning/phases/02-ai-sentence-generation/02-RESEARCH.md
  </read_first>
  <action>Create fixture files under `ankideck_generator/tests/fixtures/ai_sentence_candidates/` for at least one valid exact-three batch and one malformed batch that violates the contract from D-01/D-02 (for example: wrong candidate count or missing metadata). Update `ankideck_generator/tests/test_providers.py` to load those fixtures from disk and verify provider parsing/validation behavior against stable JSON samples instead of only inline strings. Keep fixture content natural and aligned with D-07/D-08: everyday speech, no meta-example phrasing, no gratuitous proper nouns.</action>
  <acceptance_criteria>
    - `ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json` exists
    - `ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json` exists
    - `ankideck_generator/tests/test_providers.py` reads at least one structured sentence fixture from disk
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_providers.py -q -k "structured_sentence or sentence_ai_candidates"</automated>
  </verify>
  <done>Fixture-backed provider tests can reproduce both valid and invalid AI sentence batches without hand-built inline JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture-driven AI-first routing regressions</name>
  <files>ankideck_generator/tests/test_deck_builder.py, ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json, ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json</files>
  <read_first>
    - ankideck_generator/tests/test_deck_builder.py
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/fixtures/ai_sentence_candidates/valid_batch.json
    - ankideck_generator/tests/fixtures/ai_sentence_candidates/malformed_batch.json
  </read_first>
  <behavior>
    - Test 1: malformed AI fixture triggers bounded web fallback and does not continue lexical flow with bad candidates.
    - Test 2: valid AI fixture wins without touching web fallback.
  </behavior>
  <action>Extend `ankideck_generator/tests/test_deck_builder.py` with fixture-driven routing tests that feed the Phase 2 JSON fixtures through fake provider/service seams. Add at least one regression proving malformed structured AI output causes bounded web fallback and one proving a valid structured AI batch prevents any web call. Keep the assertions concrete: inspect chosen sentence text, event counters, and whether `sentence_web_candidates()` / `sentence_web()` / `sentence_rewrite()` were called.</action>
  <acceptance_criteria>
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_process_word_uses_web_fallback_only_after_malformed_ai_batch`
    - `ankideck_generator/tests/test_deck_builder.py` contains a valid-batch no-web-call regression
    - the tests assert both selected sentence text and fallback call counts
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_providers.py ankideck_generator/tests/test_deck_builder.py -q -k "structured or sentence"</automated>
  </verify>
  <done>Phase 2 routing behavior is protected by stable fixtures covering both success and malformed-output fallback paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → tests | Test inputs must faithfully represent valid and malformed AI payloads |
| regression suite → future refactors | Tests are the guardrail preventing prompt/schema drift from silently changing routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T | test fixtures | mitigate | Store canonical valid and malformed payloads on disk so future changes cannot silently redefine the structured contract in each test body. |
| T-02-11 | R | routing regressions | mitigate | Assert sentence text, event counters, and fallback call counts so AI-first vs salvage-only behavior remains attributable and auditable. |
| T-02-12 | D | malformed AI handling | mitigate | Keep a dedicated malformed-batch regression proving bad structured output triggers bounded fallback rather than downstream crashes. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_providers.py ankideck_generator/tests/test_deck_builder.py -q -k "structured or sentence"`
</verification>

<success_criteria>
- Stable fixtures exist for valid and malformed structured AI sentence batches.
- Provider parsing and `DeckBuilder` routing are both covered by fixture-based regressions.
- Malformed AI output cannot silently regain primary-path status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-sentence-generation/02-04-SUMMARY.md`
</output>
