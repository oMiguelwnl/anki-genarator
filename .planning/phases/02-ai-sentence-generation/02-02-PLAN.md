---
phase: 02-ai-sentence-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - ankideck_generator/core/sentence_generation.py
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_sentence_generation.py
autonomous: true
requirements:
  - AIG-01
  - AIG-02
must_haves:
  truths:
    - Structured AI batches are normalized and filtered by a focused service instead of ad hoc logic inside `DeckBuilder`.
    - Wrong-language, focus-missing, and artificial candidates are rejected before lexical resolution begins.
    - `DeckBuilder` consumes a narrow sentence-generation interface instead of raw provider JSON.
  artifacts:
    - path: ankideck_generator/core/sentence_generation.py
      provides: prompt-building and candidate-validation service
      contains: SentenceGenerationService
    - path: ankideck_generator/core/deck_builder.py
      provides: service integration seam
      contains: _sentence_generation_service
    - path: ankideck_generator/tests/test_sentence_generation.py
      provides: service-level regression coverage
      contains: test_sentence_generation_service_rejects_focus_mismatch
  key_links:
    - from: ankideck_generator/core/sentence_generation.py
      to: ankideck_generator/core/providers.py
      via: structured batch request and validation
      pattern: sentence_ai_candidates
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/sentence_generation.py
      via: service seam used by sentence routing
      pattern: SentenceGenerationService
---

<objective>
Extract sentence-generation logic into a focused service that enforces the Phase 2 hard contract before `DeckBuilder` continues downstream.

Purpose: Keep provider HTTP code in `ProviderManager` while moving prompt assembly, early rejection, and candidate normalization out of the monolithic orchestrator.
Output: New sentence-generation service, `DeckBuilder` integration seam, and direct service tests.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-sentence-generation/02-CONTEXT.md
@.planning/phases/02-ai-sentence-generation/02-RESEARCH.md
@ankideck_generator/core/providers.py
@ankideck_generator/core/deck_builder.py

<interfaces>
From `ankideck_generator/core/providers.py` after Plan 01:

```python
def sentence_ai_candidates(...): ...
class StructuredSentenceBatch(BaseModel): ...
class StructuredSentenceCandidate(BaseModel): ...
```

From `ankideck_generator/core/deck_builder.py`:

```python
@dataclass
class SentenceSelectionCandidate:
    text: str
    provider_name: str
    source_kind: str
    selection_score: float
    validation_errors: list[str] = field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the sentence-generation service around the structured batch contract</name>
  <files>ankideck_generator/core/sentence_generation.py, ankideck_generator/tests/test_sentence_generation.py</files>
  <read_first>
    - ankideck_generator/core/providers.py
    - ankideck_generator/core/deck_builder.py
    - .planning/phases/02-ai-sentence-generation/02-CONTEXT.md
    - .planning/phases/02-ai-sentence-generation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the service rejects candidates whose `target_form` does not match the requested focus word.
    - Test 2: the service rejects wrong-language, focus-missing, or artificial/meta-example candidates before returning normalized candidates.
  </behavior>
  <action>Create `ankideck_generator/core/sentence_generation.py` with a focused service such as `SentenceGenerationService`. It must accept the requested focus word, language, level, requested POS, requested sense, and word-count bounds; call the structured provider method from Plan 01; and convert valid batch items into a normalized domain result for `DeckBuilder`. Implement early rejection rules from D-03, D-04, D-07, and D-08: reject blank sentences, target-form mismatch, missing focus mention, wrong-language output, proper-noun-heavy or meta-example phrasing, and other obviously synthetic patterns before lexical resolution. Return both accepted candidates and machine-readable rejection reasons so later routing can distinguish malformed vs low-yield AI runs. Add `ankideck_generator/tests/test_sentence_generation.py` with local fake providers matching repo test style.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/sentence_generation.py` contains `class SentenceGenerationService`
    - the service returns machine-readable rejection reasons for failed candidates
    - `ankideck_generator/tests/test_sentence_generation.py` contains `test_sentence_generation_service_rejects_focus_mismatch`
    - `ankideck_generator/tests/test_sentence_generation.py` contains a wrong-language or artificial-sentence rejection test
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_sentence_generation.py -q</automated>
  </verify>
  <done>A dedicated service enforces the hard sentence contract before `DeckBuilder` continues into translation/definition work.</done>
</task>

<task type="auto">
  <name>Task 2: Add a narrow `DeckBuilder` seam for the sentence service</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/core/sentence_generation.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/sentence_generation.py
    - .planning/codebase/CONCERNS.md
  </read_first>
  <action>Refactor `ankideck_generator/core/deck_builder.py` just enough to instantiate or access the new sentence-generation service through a narrow seam such as `_sentence_generation_service(...)` or `_build_sentence_candidates(...)`. Keep prompt assembly, schema parsing, and candidate prefiltering inside `ankideck_generator/core/sentence_generation.py`; keep `DeckBuilder` responsible only for orchestration, scoring, event counting, and downstream lexical flow. Do not yet invert full AI/web routing in this plan; preserve current runtime behavior aside from using the service seam as the source of structured AI candidate generation.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains a dedicated sentence-service seam
    - `ankideck_generator/core/deck_builder.py` no longer needs raw provider JSON parsing for sentence batches
    - prompt assembly and early rejection logic live in `ankideck_generator/core/sentence_generation.py`, not inline in `DeckBuilder`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_sentence_generation.py -q</automated>
  </verify>
  <done>`DeckBuilder` can call a narrow sentence-generation interface without absorbing transport or prompt details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider batch result → sentence service | Untrusted model output becomes normalized candidate data |
| sentence service → `DeckBuilder` | Pre-lexical validation decides whether downstream generation continues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `ankideck_generator/core/sentence_generation.py` | mitigate | Enforce exact target-form and focus-in-sentence checks in the service before emitting any candidate to `DeckBuilder`. |
| T-02-05 | I | sentence candidate metadata | mitigate | Preserve requested POS/sense, validation signals, and rejection reasons in normalized results so downstream phases cannot lose contract context. |
| T-02-06 | E | AI-generated text handling | mitigate | Reject wrong-language and synthetic/meta-example patterns before lexical resolution so invalid AI content cannot gain elevated trust through later stages. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_sentence_generation.py -q`
</verification>

<success_criteria>
- Sentence-generation logic is extracted into a focused service.
- Early rejection happens before lexical resolution.
- `DeckBuilder` consumes a narrow sentence service seam instead of raw structured payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-sentence-generation/02-02-SUMMARY.md`
</output>
