---
phase: 02-ai-sentence-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - AIG-03
  - AIG-04
must_haves:
  truths:
    - Sentence selection runs AI structured generation before web fallback.
    - Web candidates are used only when AI is empty, malformed, wrong-language, or low-yield.
    - Configured languages still follow the existing `wordfreq`-driven intake and bounded runtime budgets.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: AI-first sentence routing and compatibility fingerprint updates
      contains: build_sentence
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: routing and bounded-fallback regression coverage
      contains: test_process_word_uses_web_fallback_only_after_ai_low_yield
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/sentence_generation.py
      via: AI candidate generation before fallback
      pattern: SentenceGenerationService
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/run_state.py
      via: compatibility fingerprint prompt/schema inputs
      pattern: build_compatibility_fingerprint
---

<objective>
Re-route `DeckBuilder` to an AI-first sentence flow with bounded fallback while preserving the current CLI and `wordfreq` intake.

Purpose: Implement D-05/D-06 and satisfy the phase goal without broadening scope into later lexical-review work.
Output: AI-first routing, bounded web salvage, updated fingerprint inputs, and orchestration regressions.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-sentence-generation/02-CONTEXT.md
@.planning/phases/02-ai-sentence-generation/02-RESEARCH.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/core/sentence_generation.py
@ankideck_generator/tests/test_deck_builder.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
def _build_compatibility_fingerprint(self, run: RunConfig) -> CompatibilityFingerprint: ...
def _process_word_textual(...): ...
def build_sentence() -> str: ...
```

From `ankideck_generator/core/sentence_generation.py` after Plan 02:

```python
class SentenceGenerationService:
    def generate_candidates(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert sentence routing to AI-first with bounded fallback</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/sentence_generation.py
    - .planning/phases/02-ai-sentence-generation/02-CONTEXT.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: `build_sentence()` tries structured AI candidates before any web candidate call.
    - Test 2: web fallback only runs when AI produced no valid candidates because of empty, malformed, wrong-language, or low-yield output.
  </behavior>
  <action>Update `ankideck_generator/core/deck_builder.py::build_sentence()` so routing becomes: valid cache -> structured AI candidates -> bounded web/Tatoeba salvage -> optional rewrite of web candidates that fail only level/profile checks. Keep `wordfreq` intake and level preparation unchanged per AIG-03. Honor D-05 and D-06 exactly: AI must exhaust its configured bounded attempt flow before web sources are considered, and web sources remain salvage-only fallback when AI is empty, malformed, wrong-language, or low-yield. Preserve existing per-word and per-field AI budgets from `RunConfig`, preserve strong-candidate short-circuit behavior where appropriate, and add/rename event counters so the logs distinguish AI-primary attempts from salvage-only web fallback.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` requests structured AI candidates before any web sentence call
    - web fallback is conditioned on AI empty/malformed/wrong-language/low-yield outcomes
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_process_word_uses_web_fallback_only_after_ai_low_yield`
    - `ankideck_generator/tests/test_deck_builder.py` contains a test proving web is not called when AI yields a valid candidate
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "sentence_ai or sentence_rewrite or tatoeba or structured"</automated>
  </verify>
  <done>`DeckBuilder` is AI-first for sentence generation and only uses web sources as bounded salvage.</done>
</task>

<task type="auto">
  <name>Task 2: Fingerprint and log the new sentence-generation contract</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/models.py
    - ankideck_generator/core/run_state.py
  </read_first>
  <action>Extend `ankideck_generator/core/deck_builder.py::_build_compatibility_fingerprint(...)` so prompt/schema inputs include the Phase 2 sentence-generation contract: prompt version, schema version, and any sentence service validation policy identifiers introduced in Plans 01-02. Update event/reason-code logging so AI malformed batches, focus mismatches, wrong-language AI output, and AI low-yield fallback are distinguishable in `LogRecord` fields already used by the runtime. Do not change the CLI surface or non-sentence provider fallback order in this plan.</action>
  <acceptance_criteria>
    - `_build_compatibility_fingerprint(` includes sentence prompt/schema version inputs
    - `LogRecord` output from sentence generation distinguishes malformed AI output from low-yield fallback
    - non-sentence provider fallback order remains unchanged
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "sentence_ai or sentence_rewrite or tatoeba or structured"</automated>
  </verify>
  <done>The new AI-first sentence contract participates in compatibility/versioning and produces audit-friendly event data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sentence service → `DeckBuilder` routing | Candidate validity determines whether fallback runs and downstream lexical work begins |
| runtime config → AI/web routing | Untrusted config values influence external-call ordering and budget exhaustion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | D | `build_sentence()` | mitigate | Keep AI attempts bounded by existing `sentence_ai_attempts`, `ai_max_calls_per_word`, and `ai_max_calls_per_field` so malformed AI output cannot trigger runaway retries. |
| T-02-08 | T | fallback routing | mitigate | Gate web fallback on explicit AI failure categories (empty, malformed, wrong-language, low-yield) so salvage paths cannot silently become the default again. |
| T-02-09 | R | compatibility fingerprint / logs | mitigate | Include sentence prompt/schema versions and reason-coded fallback events so stale artifacts and routing regressions are attributable in run outputs. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "sentence_ai or sentence_rewrite or tatoeba or structured"`
</verification>

<success_criteria>
- AI generation is the primary sentence path.
- Web sources remain salvage-only fallback.
- The new sentence contract is versioned and observable in runtime logs/fingerprints.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-sentence-generation/02-03-SUMMARY.md`
</output>
