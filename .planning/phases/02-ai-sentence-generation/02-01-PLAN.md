---
phase: 02-ai-sentence-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ankideck_generator/core/models.py
  - ankideck_generator/core/providers.py
  - ankideck_generator/tests/test_providers.py
autonomous: true
requirements:
  - AIG-01
  - AIG-02
must_haves:
  truths:
    - AI sentence generation has a typed structured contract instead of relying on one free-text line.
    - Every AI batch is validated as exactly three candidates before `DeckBuilder` consumes it.
    - Prompt/schema version metadata exists so later routing can fingerprint sentence-generation compatibility.
  artifacts:
    - path: ankideck_generator/core/models.py
      provides: structured sentence payload contracts
      contains: StructuredSentenceBatch
    - path: ankideck_generator/core/providers.py
      provides: validated AI sentence batch transport
      contains: sentence_ai_candidates
    - path: ankideck_generator/tests/test_providers.py
      provides: transport/schema regression coverage
      contains: test_sentence_ai_candidates_rejects_malformed_payload
  key_links:
    - from: ankideck_generator/core/providers.py
      to: ankideck_generator/core/models.py
      via: Pydantic model validation for AI payloads
      pattern: StructuredSentenceBatch
    - from: ankideck_generator/tests/test_providers.py
      to: ankideck_generator/core/providers.py
      via: direct parsing and schema assertions
      pattern: sentence_ai_candidates
---

<objective>
Define the typed sentence-candidate contract and provider transport needed for Phase 2 AI generation.

Purpose: Lock D-01 through D-04 before routing changes so malformed AI outputs cannot leak into lexical resolution.
Output: Pydantic sentence-batch models, provider JSON parsing helpers, and direct transport tests.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ai-sentence-generation/02-CONTEXT.md
@.planning/phases/02-ai-sentence-generation/02-RESEARCH.md
@ankideck_generator/core/models.py
@ankideck_generator/core/providers.py

<interfaces>
From `ankideck_generator/core/models.py`:

```python
class RunConfig(BaseModel):
    sentence_ai_attempts: int = 2
    ai_max_calls_per_word: int = 6
    ai_max_calls_per_field: int = 2
```

From `ankideck_generator/core/providers.py`:

```python
@dataclass
class SentenceCandidate:
    text: str
    provider_name: str
    score: float
    source: str = "tatoeba"
    query_mode: str = "exact"

def _ai_request(self, system_prompt: str, user_prompt: str, first_line_only: bool = True) -> str | None: ...
def _extract_json_payload(text: str) -> Any | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact-three structured sentence payload models</name>
  <files>ankideck_generator/core/models.py, ankideck_generator/tests/test_providers.py</files>
  <read_first>
    - ankideck_generator/core/models.py
    - .planning/phases/02-ai-sentence-generation/02-CONTEXT.md
    - .planning/phases/02-ai-sentence-generation/02-RESEARCH.md
    - ankideck_generator/tests/test_providers.py
  </read_first>
  <behavior>
    - Test 1: the structured batch model accepts exactly 3 candidates and rejects 2 or 4.
    - Test 2: each candidate requires `sentence`, `target_form`, requested POS/sense metadata, validation signals, and rationale.
  </behavior>
  <action>Update `ankideck_generator/core/models.py` to add Phase 2 Pydantic contracts for structured AI sentence generation per D-01, D-02, and D-03. Create explicit models named `StructuredSentenceCandidate` and `StructuredSentenceBatch` (or equally specific names) with a hard exact-three constraint on `candidates`. Include batch metadata fields for prompt/schema versioning plus request metadata that later fingerprinting can consume, including the focus word, language, requested POS, requested sense, and target level. Each candidate must carry the actual sentence text, the target form used, the requested POS/sense echoed back, validation signals, and a short rationale string per D-02. Add or extend tests in `ankideck_generator/tests/test_providers.py` so malformed candidate counts and missing required metadata fail validation immediately.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/models.py` contains `class StructuredSentenceCandidate`
    - `ankideck_generator/core/models.py` contains `class StructuredSentenceBatch`
    - the batch model constrains `candidates` to exactly 3 items
    - `ankideck_generator/tests/test_providers.py` contains `test_sentence_ai_candidates_rejects_malformed_payload`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_providers.py -q -k "structured_sentence or sentence_ai_candidates"</automated>
  </verify>
  <done>Typed contracts exist for exact-three structured AI sentence batches, and malformed payload sizes fail under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provider helper for validated AI sentence batches</name>
  <files>ankideck_generator/core/providers.py, ankideck_generator/tests/test_providers.py</files>
  <read_first>
    - ankideck_generator/core/providers.py
    - ankideck_generator/core/models.py
    - .planning/phases/02-ai-sentence-generation/02-RESEARCH.md
    - ankideck_generator/tests/test_providers.py
  </read_first>
  <behavior>
    - Test 1: provider parses fenced or raw JSON and returns a validated structured batch.
    - Test 2: provider reports malformed JSON, missing fields, or wrong candidate count as an error without returning partial candidates.
  </behavior>
  <action>Extend `ankideck_generator/core/providers.py` with a dedicated helper path for structured sentence generation instead of reusing one-line `sentence_ai()`. Add a method such as `_ai_request_json(...)` or `sentence_ai_candidates(...)` that calls `_ai_request(..., first_line_only=False)`, runs `_extract_json_payload(...)`, and validates the result against `StructuredSentenceBatch` using Pydantic per the research guidance. Preserve existing `sentence_ai()` for compatibility, but add a new structured entry point for Phase 2. Populate prompt/schema version fields in the request contract, keep provider ownership in `ProviderManager`, and return explicit errors for malformed JSON, missing required fields, or non-exact candidate counts so D-04 rejections happen before `DeckBuilder` consumes anything.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/providers.py` contains `def sentence_ai_candidates(` or `def _sentence_ai_candidates(`
    - the structured helper calls `_ai_request(` with `first_line_only=False`
    - the helper validates payloads with `StructuredSentenceBatch`
    - malformed structured payloads surface as an error result instead of partial success
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_providers.py -q -k "structured_sentence or sentence_ai_candidates"</automated>
  </verify>
  <done>`ProviderManager` can request, parse, and reject structured AI sentence batches without changing unrelated provider fallbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM JSON response → `ProviderManager` | Untrusted model output enters trusted sentence-generation flow |
| prompt/schema metadata → compatibility fingerprint | Version metadata changes cache/resume compatibility decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `ankideck_generator/core/providers.py` | mitigate | Parse structured output through `_extract_json_payload(...)` plus Pydantic validation so malformed or reshaped JSON cannot spoof a valid batch. |
| T-02-02 | D | `StructuredSentenceBatch` ingestion | mitigate | Reject non-exact candidate counts and missing required fields up front so malformed payloads fail fast instead of triggering downstream retries or crashes. |
| T-02-03 | R | prompt/schema version metadata | mitigate | Persist explicit prompt/schema version fields in the typed contract so later fingerprinting can attribute incompatible outputs to a concrete contract revision. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_providers.py -q -k "structured_sentence or sentence_ai_candidates"`
</verification>

<success_criteria>
- The AI sentence path has a typed exact-three batch contract.
- Provider transport rejects malformed structured payloads before orchestration.
- Prompt/schema version metadata exists for later compatibility fingerprinting.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-sentence-generation/02-01-SUMMARY.md`
</output>
