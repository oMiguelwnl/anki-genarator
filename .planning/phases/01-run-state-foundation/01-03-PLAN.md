---
phase: 01-run-state-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
  - ankideck_generator/tests/test_deck_export.py
autonomous: true
requirements:
  - FOUND-04
  - FOUND-01
  - COMP-05
must_haves:
  truths:
    - Export writes only accepted cards into the `.apkg` package.
    - `review_queue.json` contains rejected cards only, while accepted-but-corrected cards are reflected through aggregate metrics.
    - Quality outputs preserve machine-readable reasons and provider/model provenance for rejected outcomes.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: accepted-only export and report filtering
      contains: accepted_cards
    - path: ankideck_generator/tests/test_deck_export.py
      provides: export gating regression
      contains: test_deck_export_uses_only_accepted_cards
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: review queue and quality report regressions
      contains: test_write_quality_outputs_only_rejected_cards_enter_review_queue
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: output/review_queue.json
      via: _write_quality_outputs filtering
      pattern: review_queue_path
    - from: ankideck_generator/core/deck_builder.py
      to: genanki.Package
      via: export_deck accepted-card filtering
      pattern: export_deck
---

<objective>
Finish Phase 1 by enforcing accepted-only export and tightening review artifacts to rejected-only queue semantics.

Purpose: Deliver FOUND-04 and the user-visible gating behavior promised by the roadmap.
Output: Accepted-only deck export, rejected-only review queue, richer quality metrics, and tests.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-run-state-foundation/01-CONTEXT.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/tests/test_deck_builder.py
@ankideck_generator/tests/test_deck_export.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
class BuildStats:
    cards: list[CardData]
    needs_review_items: list[dict[str, object]]

def _record_log(self, logger: JsonLogger, stats: BuildStats, log_record: LogRecord) -> None: ...
def _write_quality_outputs(self, run: RunConfig, stats: BuildStats) -> None: ...
def export_deck(self, run: RunConfig, cards: list[CardData], media_files: list[str]) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce accepted-only export path</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_export.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/test_deck_export.py
    - .planning/phases/01-run-state-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: only cards marked `accepted` are rendered into the exported `.apkg`.
    - Test 2: rejected/generated/reviewed-only items never reach export notes even if they exist in runtime stats.
  </behavior>
  <action>Update `ankideck_generator/core/deck_builder.py` so the export path uses an explicit accepted-card collection rather than assuming every `stats.cards` item is exportable. Preserve the current repo-root CLI entrypoint and `.apkg` packaging flow per COMP-05, but make `export_deck()` consume only cards whose final lifecycle state is `accepted` per FOUND-04 and D-03. Add/rename tests in `ankideck_generator/tests/test_deck_export.py` so they prove one rejected/non-final card is excluded from the generated model/package while accepted cards still export normally.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains explicit accepted-card filtering before note creation
    - `ankideck_generator/tests/test_deck_export.py` contains `test_deck_export_uses_only_accepted_cards`
    - Export tests still assert the existing default field order and template contract
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_export.py -q</automated>
  </verify>
  <done>Deck export ignores non-accepted cards while keeping the current packaging/template behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter review artifacts to rejected cards and aggregate corrected-accept metrics</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/tests/test_deck_builder.py
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - Test 1: `review_queue.json` contains rejected cards only.
    - Test 2: accepted cards with review/correction notes stay out of the queue but increment aggregate quality metrics.
  </behavior>
  <action>Revise `_record_log()` and `_write_quality_outputs()` in `ankideck_generator/core/deck_builder.py` so `stats.needs_review_items` and the written `review_queue.json` hold only `rejected` cards per D-11, while accepted cards that needed AI correction remain represented through aggregate metrics in `quality_report.json` per D-13/D-14. Preserve machine-readable `discard_reason`, `review_notes`, `providers`, `provider_errors`, `quality_scores`, and `selection_reasons` for rejected items per D-12. Add a targeted regression in `ankideck_generator/tests/test_deck_builder.py` that writes both a rejected item and an accepted-but-corrected item, then asserts the queue contains only the rejected item and the report exposes corrected acceptance counts.</action>
  <acceptance_criteria>
    - `_record_log(` no longer appends accepted items to `stats.needs_review_items`
    - `_write_quality_outputs(` writes a rejected-only queue
    - `quality_report.json` output includes an aggregate corrected-accepted metric
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_write_quality_outputs_only_rejected_cards_enter_review_queue`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs or review_queue or accepted_only"</automated>
  </verify>
  <done>Rejected-card reporting is isolated to the review queue, while accepted corrections are preserved as aggregate quality data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime card state → export package | Only trusted accepted cards may cross into the shipped deck |
| log/audit record → JSON reports | Structured but untrusted generation outcomes become persistent user-facing artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `export_deck()` | mitigate | Filter by explicit final lifecycle state before creating `genanki.Note` so generated/reviewed/rejected cards cannot leak into the `.apkg`. |
| T-01-08 | R | `review_queue.json` | mitigate | Preserve `discard_reason`, `review_notes`, provider info, and selection reasons for every rejected item written to the queue. |
| T-01-09 | D | quality output writing | mitigate | Keep report generation aggregate and file-based; do not require loading full rejected+accepted archives into memory beyond existing `BuildStats` structures. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_export.py -q`
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "quality_outputs or review_queue or accepted_only"`
</verification>

<success_criteria>
- `.apkg` export includes only accepted cards.
- `review_queue.json` contains rejected cards only.
- `quality_report.json` retains aggregate visibility into accepted cards that required correction.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-state-foundation/01-03-SUMMARY.md`
</output>
