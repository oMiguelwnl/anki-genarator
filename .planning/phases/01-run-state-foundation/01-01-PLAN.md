---
phase: 01-run-state-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ankideck_generator/core/models.py
  - ankideck_generator/core/run_state.py
  - ankideck_generator/tests/test_run_state.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - COMP-05
must_haves:
  truths:
    - Runtime names generated, reviewed, accepted, and rejected outcomes explicitly, without introducing a human-review mainline state.
    - Progress and cache compatibility can be checked with a fingerprint derived from model, prompt, schema, and validator inputs.
    - Audit contracts can carry reason codes and before/after snapshots needed by later phases.
  artifacts:
    - path: ankideck_generator/core/models.py
      provides: lifecycle and fingerprint contracts
      contains: CardLifecycleState
    - path: ankideck_generator/core/run_state.py
      provides: fingerprint and compatibility helpers
      contains: build_compatibility_fingerprint
    - path: ankideck_generator/tests/test_run_state.py
      provides: regression coverage for state contracts
      contains: test_build_compatibility_fingerprint_changes_when_versions_change
  key_links:
    - from: ankideck_generator/core/run_state.py
      to: ankideck_generator/core/models.py
      via: shared state and fingerprint types
      pattern: CompatibilityFingerprint
    - from: ankideck_generator/tests/test_run_state.py
      to: ankideck_generator/core/run_state.py
      via: direct helper assertions
      pattern: build_compatibility_fingerprint
---

<objective>
Define the compact state, audit, and compatibility contracts that Phase 1 depends on.

Purpose: Lock the foundation for D-01 through D-10 before `DeckBuilder` wiring begins.
Output: Typed lifecycle/fingerprint models, helper functions, and regression tests.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-run-state-foundation/01-CONTEXT.md
@ankideck_generator/core/models.py

<interfaces>
From `ankideck_generator/core/models.py`:

```python
class ProgressState(BaseModel):
    language: str
    mode: str
    level: int = 1
    index: int = 0
    rng_state: Any | None = None
    processed_focus: list[str] = Field(default_factory=list)
    processed_sentences: list[str] = Field(default_factory=list)
    created_cards: int = 0

class LogRecord(BaseModel):
    focus: str
    level: int
    providers: dict[str, str] = Field(default_factory=dict)
    provider_errors: dict[str, str] = Field(default_factory=dict)
    review_notes: list[str] = Field(default_factory=list)
    status: str
    discard_reason: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact lifecycle and audit contracts</name>
  <files>ankideck_generator/core/models.py, ankideck_generator/tests/test_run_state.py</files>
  <read_first>
    - ankideck_generator/core/models.py
    - .planning/phases/01-run-state-foundation/01-CONTEXT.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: lifecycle state typing accepts `generated`, `reviewed`, `accepted`, and `rejected` only.
    - Test 2: progress and audit models preserve reason codes plus before/after payload snapshots.
  </behavior>
  <action>Update `ankideck_generator/core/models.py` to introduce exact Phase 1 contract names: `CardLifecycleState = Literal["generated", "reviewed", "accepted", "rejected"]`, `CompatibilityFingerprint`, and any supporting audit model needed to carry `reason_codes`, `before`, `after`, `provider`, and `model` fields. Extend `ProgressState` so it can store a schema version and a compatibility fingerprint per D-07/D-08, and extend `LogRecord` so later plans can record final state plus structured reason codes per D-03/D-05/D-12. Do not add a `human-review` lifecycle state, and keep nuance in reason-code/history fields per D-02 and D-06. Add focused tests in `ankideck_generator/tests/test_run_state.py` that instantiate the new models directly and fail if unsupported states or missing fingerprint fields slip in.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/models.py` contains `CardLifecycleState = Literal["generated", "reviewed", "accepted", "rejected"]`
    - `ankideck_generator/core/models.py` contains `class CompatibilityFingerprint`
    - `ankideck_generator/core/models.py` contains fields for `reason_codes`, `before`, and `after` on the new audit contract or `LogRecord`
    - `ankideck_generator/tests/test_run_state.py` contains a test that rejects unsupported lifecycle states
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_run_state.py -q</automated>
  </verify>
  <done>State, fingerprint, and audit contracts exist with tests proving the compact lifecycle and structured audit payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create compatibility fingerprint helpers</name>
  <files>ankideck_generator/core/run_state.py, ankideck_generator/tests/test_run_state.py</files>
  <read_first>
    - ankideck_generator/core/models.py
    - ankideck_generator/utils/file_utils.py
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - Test 1: changing model, prompt, schema, or validator inputs changes the computed fingerprint.
    - Test 2: helper output is stable for identical inputs and normalizes dict ordering.
  </behavior>
  <action>Create `ankideck_generator/core/run_state.py` with pure helpers named `build_compatibility_fingerprint(...)`, `fingerprints_match(...)`, and `quarantine_name(...)`. `build_compatibility_fingerprint(...)` must produce a deterministic `CompatibilityFingerprint` using explicit inputs for `model`, `prompt`, `schema`, and `validator` per D-08; normalize mapping/list ordering so the same logical config yields the same fingerprint. `quarantine_name(...)` must generate a non-destructive quarantine filename for incompatible/corrupt artifacts per D-09/D-17. Expand `ankideck_generator/tests/test_run_state.py` to cover fingerprint stability, change detection, and quarantine naming.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/run_state.py` contains `def build_compatibility_fingerprint(`
    - `ankideck_generator/core/run_state.py` contains `def fingerprints_match(`
    - `ankideck_generator/core/run_state.py` contains `def quarantine_name(`
    - `ankideck_generator/tests/test_run_state.py` contains `test_build_compatibility_fingerprint_changes_when_versions_change`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_run_state.py -q</automated>
  </verify>
  <done>Pure helper seams exist for compatibility/version logic, with direct regression coverage for deterministic fingerprinting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime config → fingerprint helpers | Untrusted config/provider values become compatibility decisions |
| persisted JSON → typed models | Corrupt or stale artifact content crosses into trusted runtime state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `ankideck_generator/core/run_state.py` | mitigate | Canonicalize inputs before hashing/comparing so equivalent configs cannot spoof compatibility through key ordering. |
| T-01-02 | I | `ProgressState` / audit contracts | mitigate | Keep audit payload fields explicit (`reason_codes`, `before`, `after`, `provider`, `model`) so later logging/report plans do not drop provenance. |
| T-01-03 | D | typed model loading | mitigate | Model contracts must tolerate missing legacy fields via defaults while still exposing schema/fingerprint presence for compatibility checks. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_run_state.py -q`
</verification>

<success_criteria>
- Lifecycle states are explicit and exclude `human-review` from the mainline contract.
- Compatibility fingerprints can be computed deterministically from model/prompt/schema/validator inputs.
- Audit contracts can represent reason codes and before/after snapshots needed by later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-state-foundation/01-01-SUMMARY.md`
</output>
