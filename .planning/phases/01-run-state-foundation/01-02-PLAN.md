---
phase: 01-run-state-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - FOUND-02
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - Resume restores execution position and RNG state when the saved artifact is compatible.
    - Incompatible or corrupt progress artifacts are quarantined and bypassed instead of crashing or silently reusing stale state.
    - Progress checkpoints are written only after final accepted/rejected decisions.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: deterministic resume and decision checkpoint seams
      contains: _load_resume_state
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: resume and checkpoint regressions
      contains: test_build_resume_restores_rng_state_when_fingerprint_matches
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/run_state.py
      via: fingerprint and quarantine helpers
      pattern: build_compatibility_fingerprint
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/tests/test_deck_builder.py
      via: deterministic resume and autosave assertions
      pattern: resume_restores_rng_state
---

<objective>
Wire deterministic resume and final-decision checkpoint seams into `DeckBuilder` without changing the repo-root CLI contract.

Purpose: Make Phase 1 runtime state safe to reuse and prepare later AI services to hook into explicit decision boundaries.
Output: Resume loading/quarantine behavior, final-decision-only progress saves, and regression tests.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-run-state-foundation/01-CONTEXT.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/core/run_state.py
@ankideck_generator/tests/test_deck_builder.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
class ProgressStore:
    def load(self, language: str, mode: str) -> ProgressState | None: ...
    def save(self, state: ProgressState) -> None: ...

def build(self, run: RunConfig) -> tuple[list[CardData], list[str]]: ...
def _save_progress(..., level: int, index: int, processed_focus: set[str], processed_sentences: set[str], next_sort_index: int) -> None: ...
def _maybe_checkpoint(...) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore only compatible progress state</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/run_state.py
    - .planning/codebase/CONCERNS.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: compatible progress restores `level`, `index`, `rng_state`, processed sets, and next sort index.
    - Test 2: incompatible or corrupt progress is quarantined and generation continues from a clean state.
  </behavior>
  <action>In `ankideck_generator/core/deck_builder.py`, add a small helper seam named `_load_resume_state(...)` that uses the Plan 01 fingerprint helpers. Compute the expected fingerprint from current runtime inputs, compare it to the persisted progress artifact, and: (a) restore `level`, `index`, `rng_state`, `processed_focus`, `processed_sentences`, and `created_cards` when compatible per D-07/D-08, or (b) quarantine the bad file and continue with a fresh state when the JSON is corrupt or the fingerprint/schema is incompatible per D-09/D-17. Update `build()` so level iteration and sort-index initialization use the restored state instead of always restarting from level 1/index 0. Preserve the existing repo-root CLI/run config entrypoints per COMP-01 and keep `wordfreq` level preparation as the intake front door per COMP-02.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains `def _load_resume_state(`
    - `ankideck_generator/core/deck_builder.py` contains logic that calls `random.setstate(` when a saved RNG state is compatible
    - `ankideck_generator/core/deck_builder.py` contains quarantine handling for corrupt or incompatible progress files
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_build_resume_restores_rng_state_when_fingerprint_matches`
    - `ankideck_generator/tests/test_deck_builder.py` contains a corrupt/incompatible progress quarantine test
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "resume or progress or quarantine"</automated>
  </verify>
  <done>Resume is deterministic when compatible and self-healing when stored artifacts are stale or malformed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save progress only after final card decisions</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - .planning/phases/01-run-state-foundation/01-CONTEXT.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: autosave/checkpoint writes happen only after cards become `accepted` or `rejected`.
    - Test 2: transient generated/reviewed work does not advance saved progress.
  </behavior>
  <action>Refactor `ankideck_generator/core/deck_builder.py` to introduce a minimal seam named `_finalize_card_decision(...)` or equivalent that centralizes the final `accepted` vs `rejected` outcome, updates processed counters, and triggers `_save_progress(...)` only after a final decision per D-10 and D-15. Route both serial and parallel level builders through that seam so progress writes are no longer tied to arbitrary autosave timing mid-card. Keep the change narrow: do not decompose `ProviderManager` or rewrite unrelated generation stages per D-16 and deferred scope. Add regression tests proving that generated/reviewed intermediate work does not advance the saved resume artifact while accepted/rejected decisions do.</action>
  <acceptance_criteria>
    - `ankideck_generator/core/deck_builder.py` contains a dedicated final-decision helper used by both serial and parallel builders
    - `_save_progress(` is called from final-decision flow rather than arbitrary in-progress branches
    - `ankideck_generator/tests/test_deck_builder.py` contains a test proving checkpoints wait for final decisions
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "resume or progress or checkpoint or final_decision"</automated>
  </verify>
  <done>Progress persistence is aligned with final card outcomes and no longer records arbitrary intermediate work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| progress JSON → `DeckBuilder.build()` | Persisted user/runtime data becomes trusted resume state |
| resume helper → RNG/process control | Compatibility decision changes execution order and exported output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | progress file loading | mitigate | Validate schema/fingerprint before restoring `level`, `index`, and `rng_state`; quarantine mismatches instead of using them. |
| T-01-05 | D | `DeckBuilder.build()` | mitigate | Catch malformed JSON/model payloads during resume load and continue with a clean state so one bad file cannot abort the run. |
| T-01-06 | R | final-decision checkpoint seam | mitigate | Save progress only from a single final-decision helper so accepted/rejected outcomes are attributable and reproducible. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "resume or progress or checkpoint or quarantine"`
</verification>

<success_criteria>
- Compatible resume restores position and RNG state.
- Corrupt/incompatible progress is quarantined and bypassed.
- Saved progress reflects final accepted/rejected decisions only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-state-foundation/01-02-SUMMARY.md`
</output>
