---
phase: 03-contextual-lexical-review
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - LEX-04
  - COMP-04
must_haves:
  truths:
    - Any AI or manual lexical correction reruns deterministic validators before final acceptance.
    - Failed corrections are rejected with evidence instead of bypassing acceptance gates.
    - Audio remains post-acceptance and is not generated for cards that fail post-correction revalidation.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: post-correction revalidation and audio-after-acceptance ordering
      contains: _process_word
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: correction revalidation regressions
      contains: test_interactive_edit_reruns_validation_before_acceptance
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/validators.py
      via: `_validate_text_candidate()` after AI/manual edits
      pattern: validate_card
    - from: ankideck_generator/core/deck_builder.py
      to: `ProviderManager.audio()`
      via: `_attach_audio_to_card()` only after corrected text clears revalidation
      pattern: _attach_audio_to_card
---

<objective>
Enforce deterministic revalidation after any lexical correction and keep audio strictly post-acceptance.

Purpose: Close D-13 through D-16 and the known interactive-edit validation gap so corrected cards cannot bypass the same acceptance bar as the rest of the pipeline.
Output: a single post-correction validation seam, reordered acceptance/audio gating, and regression coverage for AI/manual correction flows.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-contextual-lexical-review/03-CONTEXT.md
@.planning/phases/03-contextual-lexical-review/03-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/phases/03-contextual-lexical-review/03-02-SUMMARY.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/core/validators.py
@ankideck_generator/tests/test_deck_builder.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
def _process_word(... ) -> tuple[CardData | None, LogRecord]: ...
def _validate_text_candidate(... ) -> list[str]: ...
def _audio_validation_errors(... ) -> list[str]: ...
def _interactive_edit(self, card: CardData, log_record: LogRecord) -> CardData: ...
```

From `ankideck_generator/core/validators.py`:

```python
def validate_card(
    card: CardData,
    ctx: ValidationContext,
    validations: dict[str, object],
    *,
    commit: bool = True,
) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate every AI or manual lexical correction before acceptance</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/validators.py
    - .planning/codebase/CONCERNS.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: an AI-corrected card that still fails deterministic validation is rejected with reason codes and never accepted per D-13/D-16.
    - Test 2: an interactive/manual edit reruns the same validator path and cannot bypass acceptance per D-15.
  </behavior>
  <action>Refactor `ankideck_generator/core/deck_builder.py::_process_word()` so all correction paths converge through one deterministic revalidation seam that calls `_validate_text_candidate()` after lexical review corrections and again after `_interactive_edit()` when interactive mode changes text fields. Use the existing hard/soft error policy from `_should_reject_errors()` per D-14; do not create looser review-only rules. If revalidation fails after an AI or manual correction, mark the card `rejected`, keep the before/after evidence and reason codes, and route it through the same rejected artifact path per D-16. This task must close the known bug from `.planning/codebase/CONCERNS.md` where interactive edits currently bypass final validation.</action>
  <acceptance_criteria>
    - `_process_word(` reruns `_validate_text_candidate(` after lexical review corrections
    - `_process_word(` reruns `_validate_text_candidate(` after `_interactive_edit(` when edits occur
    - failed post-correction validation sets `lifecycle_state="rejected"`
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_interactive_edit_reruns_validation_before_acceptance`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "revalidation or interactive_edit"</automated>
  </verify>
  <done>No AI or manual correction can bypass the existing deterministic validator suite before acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep audio post-acceptance for corrected cards</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - AGENTS.md
    - .planning/ROADMAP.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: corrected cards that fail post-review revalidation never call `_attach_audio_to_card()`.
    - Test 2: cards that pass post-review revalidation still generate audio and remain compatible with the existing export path per COMP-04.
  </behavior>
  <action>Reorder `ankideck_generator/core/deck_builder.py::_process_word()` so corrected-card text acceptance is decided before audio generation, keeping audio/TTS as a post-acceptance step per the Phase 3 goal and COMP-04. Specifically: text generation -> lexical review -> post-correction revalidation -> final text acceptance decision -> `_attach_audio_to_card()` -> audio validation -> accepted card. Ensure cards rejected by post-correction validation never reach `_attach_audio_to_card()`, but cards that pass still use the existing audio providers and export-compatible card fields from `CardData`/`export_deck()`. Add regressions proving both the blocked-audio and accepted-audio paths.</action>
  <acceptance_criteria>
    - `_attach_audio_to_card(` is called only after corrected text passes revalidation
    - corrected cards rejected by post-review validation do not generate audio
    - `ankideck_generator/tests/test_deck_builder.py` contains a regression asserting no audio call on failed corrected cards
    - `ankideck_generator/tests/test_deck_builder.py` contains a regression asserting audio still runs on successful corrected cards
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "audio and lexical_review"</automated>
  </verify>
  <done>Corrected cards only reach audio after clearing deterministic validation, and successful accepted cards still preserve the existing audio/export integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual edit input → `CardData` | Human input mutates trusted card fields and must be revalidated |
| accepted text → audio provider | Only post-acceptance content should cross into TTS/audio generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | `_interactive_edit()` / correction flow | mitigate | Route all AI/manual mutations back through `_validate_text_candidate()` and `_should_reject_errors()` so edited content cannot tamper with final acceptance state. |
| T-03-08 | E | acceptance gating | mitigate | Reuse the existing hard/soft validator policy after correction so manual edits do not gain a looser privilege path than AI or untouched cards. |
| T-03-09 | D | audio generation path | mitigate | Call `_attach_audio_to_card()` only after corrected text passes revalidation so invalid cards cannot waste audio/TTS work or leak into export with media attached. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "revalidation or interactive_edit"`
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "audio and lexical_review"`
</verification>

<success_criteria>
- Any AI/manual correction reruns deterministic validators before acceptance.
- Failed corrections are rejected with evidence instead of bypassing the gate.
- Audio remains post-acceptance and compatible with the existing export pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-contextual-lexical-review/03-03-SUMMARY.md`
</output>
