---
phase: 03-contextual-lexical-review
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - ankideck_generator/core/deck_builder.py
  - ankideck_generator/core/lexical_review.py
  - ankideck_generator/tests/test_deck_builder.py
autonomous: true
requirements:
  - LEX-01
  - LEX-02
  - LEX-03
must_haves:
  truths:
    - Translation and definition are reviewed against the accepted sentence and fixed focus word.
    - Fixable lexical issues are auto-corrected without blocking the run.
    - Unresolved low-confidence cards are rejected into queue/report artifacts with structured reason codes and before/after evidence.
  artifacts:
    - path: ankideck_generator/core/deck_builder.py
      provides: lexical review orchestration and queue routing
      contains: _process_word_textual
    - path: ankideck_generator/core/lexical_review.py
      provides: sentence-anchored correction selection
      contains: review_card
    - path: ankideck_generator/tests/test_deck_builder.py
      provides: end-to-end lexical review regressions
      contains: test_process_word_auto_corrects_definition_without_rewriting_sentence
  key_links:
    - from: ankideck_generator/core/deck_builder.py
      to: ankideck_generator/core/lexical_review.py
      via: review call built from accepted sentence and lexical candidates
      pattern: LexicalReviewService
    - from: ankideck_generator/core/deck_builder.py
      to: output/review_queue.json
      via: rejected cards flow through existing queue artifact path
      pattern: needs_review_items
---

<objective>
Integrate sentence-anchored lexical review and correction into `DeckBuilder` without introducing a blocking runtime human-review lane.

Purpose: Implement D-01 through D-12 in orchestration so fixable lexical issues auto-correct, unresolved ambiguity rejects cleanly, and audit evidence survives in current output artifacts.
Output: `DeckBuilder` lexical-review seam, correction/reject routing, and regression coverage for auto-correct vs reject behavior.
</objective>

<execution_context>
@C:/dev/script-dev/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/script-dev/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-contextual-lexical-review/03-CONTEXT.md
@.planning/phases/03-contextual-lexical-review/03-RESEARCH.md
@.planning/phases/03-contextual-lexical-review/03-PATTERNS.md
@.planning/phases/03-contextual-lexical-review/03-01-SUMMARY.md
@ankideck_generator/core/deck_builder.py
@ankideck_generator/core/lexical_review.py

<interfaces>
From `ankideck_generator/core/deck_builder.py`:

```python
def _process_word_textual(... ) -> tuple[CardData | None, LogRecord]: ...

def _record_log(self, logger: JsonLogger, stats: BuildStats, log_record: LogRecord) -> None: ...
```

From `ankideck_generator/core/models.py` after Plan 01:

```python
class LexicalReviewResult(BaseModel):
    verdict: Literal["accept", "correct", "reject"]
    reason_codes: list[str]
    corrected_definition: str | None = None
    corrected_translation: str | None = None
    winning_sense: str | None = None
    losing_sense_candidates: list[str] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply sentence-anchored lexical review inside `_process_word_textual()`</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/core/lexical_review.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/lexical_review.py
    - .planning/phases/03-contextual-lexical-review/03-CONTEXT.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: when only the definition is misaligned, lexical review corrects the definition and preserves the accepted sentence and translation per D-06/D-07/D-08.
    - Test 2: when current lexical fields already match the accepted sentence, review returns accept and no needless regeneration occurs.
  </behavior>
  <action>Update `ankideck_generator/core/deck_builder.py::_process_word_textual()` to call `LexicalReviewService` after sentence selection and initial lexical field assembly, using the accepted sentence as the immutable anchor per D-06 and the existing focus word as a locked value per D-08. Build the review request from the current `definition`, `translation`, `source_definition`, candidate previews, and any sense evidence already collected. Apply `accept` or `correct` verdicts from the service without reopening sentence generation ownership per D-05. When the service returns `correct`, patch only the wrong field when possible per D-07 instead of rebuilding both lexical fields. Do not rewrite the sentence in this plan; keep the review scoped to `definition` and `translation` only.</action>
  <acceptance_criteria>
    - `_process_word_textual(` calls the lexical review service after sentence selection
    - the accepted sentence text is preserved when lexical review applies a correction
    - the focus word remains unchanged by lexical review
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_process_word_auto_corrects_definition_without_rewriting_sentence`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "lexical_review and sentence_anchor"</automated>
  </verify>
  <done>`DeckBuilder` performs sentence-anchored lexical review and minimal field patching without retaking sentence generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route unresolved lexical cases to rejected queue artifacts with audit evidence</name>
  <files>ankideck_generator/core/deck_builder.py, ankideck_generator/tests/test_deck_builder.py</files>
  <read_first>
    - ankideck_generator/core/deck_builder.py
    - ankideck_generator/core/models.py
    - .planning/phases/01-run-state-foundation/01-03-SUMMARY.md
    - ankideck_generator/tests/test_deck_builder.py
  </read_first>
  <behavior>
    - Test 1: low-confidence ambiguity rejects the card, records reason codes, and adds before/after or winning/losing-sense evidence to audit fields.
    - Test 2: fixable lexical issues auto-correct and continue without creating a runtime `human-review` state per D-01/D-03.
  </behavior>
  <action>Extend `ankideck_generator/core/deck_builder.py` so lexical-review `reject` verdicts become standard rejected outcomes that feed the existing rejected-card path and `stats.needs_review_items`, not a new lifecycle state. Populate `LogRecord.reason_codes`, `LogRecord.before`, `LogRecord.after`, `LogRecord.review_notes`, `LogRecord.selection_reasons`, and `LogRecord.candidate_preview` with concrete evidence per D-04 and D-11: the original lexical fields, corrected proposal if any, winning-sense rationale, losing-sense candidates, and the structured reason codes that caused rejection. Ensure D-02 and D-12 are implemented exactly: low-confidence lexical cases go to `review_queue.json` through the existing rejected-queue contract, and unattended runs never block on a manual prompt.</action>
  <acceptance_criteria>
    - rejected lexical-review outcomes use `lifecycle_state="rejected"`, not `human-review`
    - rejected lexical-review outcomes populate `reason_codes`
    - `before` and `after` snapshots are present for corrected or rejected review outcomes
    - `ankideck_generator/tests/test_deck_builder.py` contains `test_process_word_rejects_low_confidence_ambiguity_to_review_queue`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "review_queue or lexical_review_reject"</automated>
  </verify>
  <done>Auto-correctable cards keep moving, and unresolved lexical cases reject into existing queue/report artifacts with full evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical-review result → `DeckBuilder` | Review verdict decides whether card content is mutated, accepted, or rejected |
| rejected-card audit data → output artifacts | Before/after snapshots and reason codes become durable follow-up evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `ankideck_generator/core/deck_builder.py` | mitigate | Keep the accepted sentence and focus word immutable during lexical correction so review cannot tamper with upstream sentence acceptance. |
| T-03-05 | R | queue/audit routing | mitigate | Persist `reason_codes`, `before`, `after`, `winning_sense`, and losing-sense evidence in `LogRecord`/queue items so rejects are attributable and replayable. |
| T-03-06 | E | runtime state handling | mitigate | Map low-confidence review to standard `rejected` output flow instead of creating a bypass or special privileged runtime state. |
</threat_model>

<verification>
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "lexical_review and sentence_anchor"`
- `python -m pytest ankideck_generator/tests/test_deck_builder.py -q -k "review_queue or lexical_review_reject"`
</verification>

<success_criteria>
- Translation/definition review is sentence-anchored inside `DeckBuilder`.
- Fixable lexical issues auto-correct without blocking the run.
- Unresolved lexical ambiguity is rejected into existing queue/audit artifacts with structured evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-contextual-lexical-review/03-02-SUMMARY.md`
</output>
